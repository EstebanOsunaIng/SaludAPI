<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp1"
    x:Class="MauiApp1.AppShell">

    <FlyoutItem Title="Inicio" Icon="home.png">
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Citas" Icon="list.png">
        <ShellContent Route="CitasPage" ContentTemplate="{DataTemplate local:CitasPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Nueva Cita" Icon="add.png">
        <ShellContent Route="NuevaCitaPage" ContentTemplate="{DataTemplate local:NuevaCitaPage}" />
    </FlyoutItem>

    <!-- ✅ Agregar Pacientes -->
    <FlyoutItem Title="Pacientes" Icon="list.png">
        <ShellContent Route="PacientesPage" ContentTemplate="{DataTemplate local:PacientesPage}" />
    </FlyoutItem>

</Shell>