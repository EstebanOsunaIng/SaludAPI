<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SaludAPI.AppShell">

    <!-- Menú de navegación lateral o inferior -->
    <FlyoutItem Title="Inicio" Icon="home.png">
        <ShellContent Title="Inicio" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Citas" Icon="calendar.png">
        <ShellContent Title="Lista de Citas" ContentTemplate="{DataTemplate local:CitasPage}" />
    </FlyoutItem>

</Shell>
